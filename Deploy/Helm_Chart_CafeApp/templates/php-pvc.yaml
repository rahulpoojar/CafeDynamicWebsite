{{- if .Values.php.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mompopcafe.fullname" . }}-php-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.php.persistence.storage }}
---
{{- end }}

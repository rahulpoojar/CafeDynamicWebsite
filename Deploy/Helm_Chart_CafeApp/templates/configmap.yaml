apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  getAppParameters.php: |
    <?php
    $showServerInfo = "true";
    $timeZone = "America/New_York";
    $currency = "£";
    $db_url = "mysql";
    $db_name = "mom_pop_db";
    $db_user = "root";
    $db_password = "{{ .Values.mysql.rootPassword }}";
